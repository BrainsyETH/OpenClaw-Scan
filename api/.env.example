# OpenClaw-Scan API Configuration
# Copy to .env and fill in your values

# Server configuration
PORT=8000
NODE_ENV=development  # development or production

# x402 Payment Configuration
WALLET_ADDRESS=0xYourWalletAddressHere  # EVM wallet to receive payments
NETWORK=base-sepolia  # base-sepolia (testnet) or base (mainnet)
PREMIUM_PRICE=$0.75  # Price per premium scan
FACILITATOR_URL=https://x402.org/facilitator  # x402 facilitator endpoint

# Coinbase CDP Facilitator (for production mainnet)
# CDP_API_KEY_ID=your-key-id-here
# CDP_API_KEY_SECRET=your-key-secret-here

# Attestation Signing (ECC private key for signing scan results)
# ATTESTATION_PRIVATE_KEY=0xYourPrivateKeyHere  # KEEP SECRET! Use secrets manager in production

# Database (optional, for scan queue and attestation storage)
# REDIS_URL=redis://localhost:6379/0
# DATABASE_URL=postgresql://user:pass@localhost/openclaw_scan

# Monitoring (optional)
# SENTRY_DSN=https://your-sentry-dsn-here

# CORS (allowed origins, comma-separated)
# ALLOWED_ORIGINS=https://openclaw-scan.com,https://moltbook.com

# Rate Limiting
# FREE_TIER_RATE_LIMIT=10  # Max free scans per hour per IP
# PREMIUM_TIER_RATE_LIMIT=100  # Max premium scans per hour
