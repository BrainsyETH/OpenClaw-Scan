# ClawdHub Scanner x402 Configuration
# Copy to .env and fill in your values

# ── Payment Settings ──────────────────────────────────────────
# Your EVM wallet address to receive USDC payments
# Leave empty to run in demo mode (all endpoints free)
PAY_TO_ADDRESS=

# Network for payment settlement (CAIP-2 format)
# Testnet: eip155:84532 (Base Sepolia)
# Mainnet: eip155:8453 (Base)
X402_NETWORK=eip155:84532

# x402 facilitator URL
# Testnet: https://x402.org/facilitator (no auth required)
# Mainnet: Use Coinbase CDP facilitator
X402_FACILITATOR_URL=https://x402.org/facilitator

# ── Pricing ───────────────────────────────────────────────────
# Price per deep scan (USDC). Format: "$0.01"
DEEP_SCAN_PRICE=$0.01

# Price per manifest scan (free by default)
MANIFEST_SCAN_PRICE=$0.00

# ── Server Settings ───────────────────────────────────────────
API_HOST=0.0.0.0
API_PORT=8402

# Temp directory for uploaded skill archives
UPLOAD_DIR=/tmp/clawdhub_scans
